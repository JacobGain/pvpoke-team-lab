# Browser path used to read the existing PvPoke application and its data.
VITE_PVPOKE_BASE_URL=/pvpoke/src

# Development-only target for Vite's /pvpoke proxy.
PVPOKE_DEV_PROXY_TARGET=http://localhost

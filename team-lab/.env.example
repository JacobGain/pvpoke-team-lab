# Optional deployment base path, for example /team-lab/.
# PvPoke-derived data and engine assets are bundled beneath this path.
VITE_BASE_PATH=/

# Normal production builds must leave diagnostics disabled.
# Use `npm run build:admin` for the explicit diagnostics-enabled target.
VITE_ENABLE_DIAGNOSTICS=false
